import React from 'react';
import CustomNavbar from '../../lib/CustomNavbar/CustomNavbar';

const Saved = (props) => {
  return (
    <div>
      Saved
      <CustomNavbar />
    </div>
  )
}

export default Saved;