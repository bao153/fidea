import React from 'react';
import CustomNavbar from '../../lib/CustomNavbar/CustomNavbar';

const Profile = (props) => {
  return (
    <div>
      Profile
      <CustomNavbar />
    </div>
  )
}

export default Profile;